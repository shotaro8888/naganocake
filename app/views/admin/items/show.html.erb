<h1>商品詳細</h1>
    <%= image_tag @item.get_image(200,100) %>
  <p>商品名</p>
    <%= @item.name %>
  <p>商品説明</p>
    <%= @item.introduction %>
  <p>ジャンル</p>
    <%= @item.genre.name %>
  <p>税込価格（税抜価格）</p>
    <%= @item.price %>
  <p>販売ステータス</p>
    <%= @item.is_active_i18n %>
  <p>
    <%= link_to edit_admin_item_path(@item) do %>編集する<% end %>
  </p>